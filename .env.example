# Server
PORT=3000
NODE_ENV=production
JWT_SECRET=change_me_to_a_long_random_string
# Postgres
DATABASE_URL=postgres://user:password@localhost:5432/olibranch
# Used to encrypt sensitive fields (e.g., payment links) at rest
DATA_ENCRYPTION_KEY=change_me_to_a_32+_char_random_string